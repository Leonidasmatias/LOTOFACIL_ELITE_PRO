# Dependencias EXCLUSIVAS de desenvolvimento (testes, lint, tipagem).
# Nao inclui as dependencias de producao — instale as duas por separado:
#
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt
#
pytest>=7.4
pytest-cov>=4.1
mypy>=1.8
ruff>=0.4
